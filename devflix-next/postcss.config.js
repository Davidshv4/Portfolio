/**
 * PostCSS configuration for Tailwind CSS processing.
 * Ensures @tailwind directives in CSS are recognized and compiled.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};


